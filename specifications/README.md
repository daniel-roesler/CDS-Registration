# CDS Registration Working Group (WG1) Specifications

This is the location that contains the specifications for the CDS Registration Working Group (WG1) (see the `.md` files in this folder).

Folder structure: `/specifications/{spec_id}/{version}/{spec_id}.md`

Each specification has its own folder, which then contains the versions of that specification.

The `dev` version is WG1's latest draft that is open to pull requests.

